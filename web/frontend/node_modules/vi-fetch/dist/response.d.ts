import { HeadersMock } from './headers';
import './appeanable';

declare class ResponseMock implements Response {
    private path;
    value: unknown;
    private init;
    body: null;
    bodyUsed: boolean;
    constructor(path: string, value: unknown, init: ResponseInit);
    private assertBodyUsed;
    json(): Promise<any>;
    text(): Promise<string>;
    formData(): Promise<FormData>;
    blob(): Promise<Blob>;
    arrayBuffer(): Promise<ArrayBuffer>;
    get headers(): HeadersMock;
    get ok(): boolean;
    get redirected(): boolean;
    get status(): number;
    get statusText(): string;
    get type(): "default";
    get url(): string;
    clone(): ResponseMock;
}

export { ResponseMock };
