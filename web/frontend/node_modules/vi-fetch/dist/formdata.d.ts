import { Appeandable } from './appeanable';

declare class FormDataMock extends Appeandable implements FormData {
    constructor(form?: HTMLFormElement);
    getAll(name: string): FormDataEntryValue[];
}

export { FormDataMock };
