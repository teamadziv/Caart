/// <reference types="node" resolution-mode="require"/>
import { ExtensionInstance } from '../../models/extensions/extension-instance.js';
import { Writable } from 'stream';
interface UpdateExtensionDraftOptions {
    extension: ExtensionInstance;
    token: string;
    apiKey: string;
    registrationId: string;
    stdout: Writable;
    stderr: Writable;
    unifiedDeployment: boolean;
}
export declare function updateExtensionDraft({ extension, token, apiKey, registrationId, stdout, stderr, unifiedDeployment, }: UpdateExtensionDraftOptions): Promise<void>;
interface UpdateExtensionConfigOptions {
    extension: ExtensionInstance;
    token: string;
    apiKey: string;
    registrationId: string;
    stdout: Writable;
    stderr: Writable;
    unifiedDeployment: boolean;
}
export declare function updateExtensionConfig({ extension, token, apiKey, registrationId, stdout, stderr, unifiedDeployment, }: UpdateExtensionConfigOptions): Promise<void>;
export {};
